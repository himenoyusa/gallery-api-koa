var config = {
  database: {
    database: "test",
    username: "user",
    password: "password",
    host: "localhost",
    port: 3306
  },
  environment: "dev" // or prod
};

module.exports = config;
